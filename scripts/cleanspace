#!/bin/bash
# 🧹 NOIZYLAB CLEANSPACE - Problem Sourcer
# Usage: cleanspace [list|show|last|heal|test]

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/cleanspace.py" "$@"
