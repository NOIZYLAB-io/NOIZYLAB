# Voice trigger scaffolding for install/repair flows
# Usage: "install OMEN", "repair Inspiron"
flows:
  install_OMEN:
    steps:
      - Prep Install Media
      - BIOS/UEFI Rituals
      - Windows 10 Install
      - Post-Install Drivers
    log: cockpit_fallback_log.yaml
  repair_Inspiron:
    steps:
      - Prep Install Media
      - BIOS/UEFI Rituals
      - Windows 10 Install
      - Post-Install Drivers
    log: cockpit_fallback_log.yaml
  install_D-LINK:
    steps:
      - Prep Install Media
      - BIOS/UEFI Rituals
      - Windows 10 Install
      - Post-Install Drivers
    log: cockpit_fallback_log.yaml
